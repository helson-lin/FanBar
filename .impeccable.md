## Design Context

### Users
FanBar 面向希望快速确认 Mac 散热状态、并在需要时手动干预风扇策略的日常与进阶用户。菜单栏是「打开即读」的主界面；**设置窗口的首要任务是配置智能温控曲线**（温度→转速），其次才是菜单栏显示、散热预设可见性、通知与语言。用户在设置里的心态是「精确调参、可信、可回退」，不是浏览营销页。

### Brand Personality
轻盈、精密、可信。界面应像 macOS 自带的硬件仪表与系统设置：信息明确、触感轻盈，不用夸张装饰制造「专业感」。克制来自更少的视觉重量，而不是隐藏功能；文案中英双语、语气冷静务实。

### Aesthetic Direction
- **设置窗口**：向 **macOS 系统设置** 靠拢——沿用 0.4.1 的标题栏图标导航：三个固定宽度图标置于原生分段控件中，以系统选中态明确当前位置，不额外占用正文高度。搭配分组列表/inset 卡片、标准 Toggle/Picker/Stepper、克制边距与二级说明脚注；浅色/深色均跟随系统外观。
- **菜单栏面板**：保留 FanBar 的精密仪表识别度（等宽 RPM、双风扇并排、有含义的叶片运动），但不把设置页做成同一套「仪表仪表盘」。
- **曲线可视化**：温控曲线是散热 Tab 的视觉焦点（平滑曲线 + 完整刻度），但整体仍是设置控件的一部分，不是独立数据大屏。
- **Anti-references**：网页风厚重卡片堆叠、多层玻璃与大阴影、炫彩/霓虹渐变、营销式区块；不为酷炫牺牲 reduce-motion、可读标签与点击区域。

### Design Principles
1. **设置以曲线为先**：打开设置（尤其是散热 Tab）时，智能温控曲线应处于第一视觉与交互层级；菜单栏/通用为平级但次要任务。
2. **主路径短、高级可展**：曲线图 + 预设 + 温度来源保持常显；锚点表、磁滞、每步限速等放在「高级」折叠区——能力不藏死，默认不抢视线。
3. **原生而不平庸**：遵循 macOS 材质、SF 字体、系统 accent 与间距习惯；用清晰层级与等宽数值建立辨识度，而不是自定义皮肤。
4. **安全可感知**：0% 停转、Helper 权限、自动恢复等与安全相关的状态必须说清楚；危险能力可强调后果，但不可为「简洁」而难找。
5. **无障碍默认开启**：支持减少动态效果、动态系统颜色、可读标签与足够点击区域；拖拽曲线必须有步进器等键盘/精确替代。
6. **轻量导航，去向始终可见**：设置页使用 0.4.1 风格的原生标题栏图标分段控件；三个入口固定宽度、完整稳定，工具提示提供文字含义，把第一视觉层级留给当前页面内容。

### Settings UI Notes (current product intent)
- Tabs：菜单栏 · 散热 · 通用（标题栏内的原生图标分段控件，持久化选中项；固定宽度并提供本地化工具提示）。
- 散热主路径：曲线画布（可拖锚点）→ 内置预设 → 温度来源 → 菜单可见的散热预设。
- 散热高级（默认折叠）：锚点列表、降温磁滞、每步最大变化。
- 窗口宽约 460pt；高度随内容 fitting，避免无意义空白，也避免一屏塞满无分区的长表单。
